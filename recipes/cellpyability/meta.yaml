{% set name = "cellpyability" %}
{% set version = "0.2.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 72823a27800e736492893e66fd8f1ea242901b1cda0bbf5f5f4173391b9afa04

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  run_exports:
    - {{ pin_subpackage(name, max_pin="x.x") }}

requirements:
  host:
    - python >=3.8
    - pip
    - setuptools
    - hatchling
  run:
    - python >=3.8
    - matplotlib-base >=3.5.0,<4.0.0
    - numpy >=1.24.4,<2.0.0
    - pandas >=2.0.0,<3.0.0
    - plotly >=5.0.0,<6.0.0
    - scipy >=1.10.0,<2.0.0
test:
  imports:
    - cellpyability
  commands:
    - cellpyability --help

about:
  home: https://github.com/bindralab/cellpyability
  summary: A Python package for analyzing cell viability data
  license: MIT
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - jlelia  # GitHub
