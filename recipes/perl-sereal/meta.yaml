{% set name = "perl-sereal" %}
{% set version = "5.006" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://cpan.metacpan.org/authors/id/Y/YV/YVES/Sereal-{{ version }}.tar.gz
  sha256: bb05e763ed6bcbea44c79217fef0b2d3918ac1547194723038c6c447ee6f59de

build:
  number: 0
  noarch: generic
  run_exports:
    - {{ pin_subpackage('perl-sereal', max_pin="x") }}

requirements:
  build:
    - make
  host:
    - perl
    - perl-extutils-makemaker
    - perl-pathtools
    - perl-scalar-list-utils
    - perl-test-deep 
    - perl-test-differences
    - perl-test-longstring
    - perl-test-simple
    - perl-test-warn
    - perl-sereal-decoder >={{ version }}
    - perl-sereal-encoder >={{ version }}
  run:
    - perl
    - perl-sereal-decoder >={{ version }}
    - perl-sereal-encoder >={{ version }}

test:
  imports:
    - Sereal

about:
  home: "https://metacpan.org/pod/Sereal"
  license: "Perl_5"
  summary: "Fast, compact, powerful binary (de-)serialization."
  doc_url: "https://metacpan.org/pod/Sereal"
